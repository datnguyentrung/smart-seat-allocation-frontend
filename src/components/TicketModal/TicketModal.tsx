import React from "react";
import {QRCodeSVG} from 'qrcode.react';
import type {TicketResponse} from "@types/types";

export default function TicketModal({ticket}: {ticket: TicketResponse}) {
  return (
    <div>
      
    </div>
  )
}
